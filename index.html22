<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>IA para Estudios y Edición de Videos - ChatGPT</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      max-width: 700px;
      margin: auto;
      padding: 1rem;
      background: #f0f4f8;
      color: #222;
    }
    h1 {
      color: #006d77;
      text-align: center;
    }
    section {
      background: white;
      border-radius: 8px;
      padding: 1rem 1.5rem;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      margin-bottom: 1.5rem;
    }
    label {
      font-weight: bold;
      display: block;
      margin-bottom: 0.5rem;
    }
    select, textarea, button {
      width: 100%;
      padding: 0.5rem;
      margin-bottom: 1rem;
      border-radius: 5px;
      border: 1px solid #ccc;
      font-size: 1rem;
    }
    button {
      background-color: #006d77;
      color: white;
      border: none;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    button:hover {
      background-color: #00555a;
    }
    #response {
      white-space: pre-wrap;
      background: #e0f7fa;
      padding: 1rem;
      border-radius: 6px;
      min-height: 100px;
    }
  </style>
</head>
<body>

  <h1>Asistente IA para Estudios y Edición de Videos</h1>

  <section>
    <label for="topic">Selecciona el área:</label>
    <select id="topic">
      <option value="estudios">Estudios</option>
      <option value="edicion">Edición de Videos</option>
    </select>

    <label for="question">Escribe tu pregunta o petición:</label>
    <textarea id="question" rows="4" placeholder="Ejemplo: ¿Cómo hago un resumen efectivo?"></textarea>

    <button id="sendBtn">Enviar a la IA</button>

    <h3>Respuesta:</h3>
    <div id="response">Aquí aparecerá la respuesta de la IA.</div>
  </section>

  <script>
    const sendBtn = document.getElementById('sendBtn');
    const responseDiv = document.getElementById('response');

    sendBtn.addEventListener('click', async () => {
      const topic = document.getElementById('topic').value;
      const question = document.getElementById('question').value.trim();

      if (!question) {
        alert('Por favor escribe una pregunta o petición.');
        return;
      }

      responseDiv.textContent = "Cargando respuesta...";

      try {
        const res = await fetch('/api/chat', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json'
          },
          body: JSON.stringify({ topic, question })
        });

        if (!res.ok) throw new Error('Error en la petición a la IA');

        const data = await res.json();
        responseDiv.textContent = data.answer;
      } catch (err) {
        responseDiv.textContent = "Hubo un error al obtener la respuesta.";
        console.error(err);
      }
    });
  </script>

</body>
</html>
